{
  "name": "resala",
  "description": "Resala App",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "shared",
    "dashboard",
    "web"
  ],
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "lint": "prettier . --write",
    "lint:check": "prettier . --check",
    "build:server": "npm run build --prefix server",
    "build:shared": "npm run build --prefix shared",
    "build:dashboard": "npm run build --prefix dashboard",
    "build": "npm i && npm-run-all build:server",
    "test:server": "npm run test --prefix server",
    "test:dashboard": "npm run test --prefix dashboard",
    "test": "npm-run-all --parallel test:server",
    "start:dashboard": "npm run dev --prefix dashboard",
    "start:server": "npm run dev --prefix server"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  }
}
