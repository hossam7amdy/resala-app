{
  "name": "resala",
  "description": "Resala App",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "shared",
    "dashboard"
  ],
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "lint": "prettier . --write",
    "lint:check": "prettier . --check",
    "build:server": "cd server && npm i && npm run build",
    "build:shared": "cd shared && npm i && npm run build",
    "build:dashboard": "cd dashboard && npm i && npm run build",
    "build": "npm i && npm-run-all build:shared build:server build:dashboard",
    "test:server": "cd server && npm run test",
    "test:dashboard": "cd dashboard && npm run test",
    "test": "npm-run-all test:server test:dashboard",
    "start:server": "cd server && npm run dev",
    "start:dashboard": "cd dashboard && npm run dev",
    "dev": "npm-run-all --parallel start:server start:dashboard"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}
