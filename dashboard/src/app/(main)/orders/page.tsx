import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Orders',
};

const OrderPage = () => {
  return (
    <main>
      <h1>Orders Page</h1>
    </main>
  );
};

export default OrderPage;
