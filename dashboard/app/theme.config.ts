import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    colorPrimary: '#026375',
  },
  components: {
    Layout: {
      siderBg: 'transparent',
    },
  },
};

export default theme;
