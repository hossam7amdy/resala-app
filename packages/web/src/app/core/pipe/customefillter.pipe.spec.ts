import { CustomefillterPipe } from './customefillter.pipe';

describe('CustomefillterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomefillterPipe();
    expect(pipe).toBeTruthy();
  });
});
